# tslint 5 and its two rule plugins declare a peer of TypeScript 3, while the project compiles on TypeScript 4, so npm's strict peer check has to be relaxed for the tree to install at all.
legacy-peer-deps=true
